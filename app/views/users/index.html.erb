<% @users.each do |user| %>


<div>
  <%= image_tag user.get_profile_image %>
  <p><%= @users.name %></p>
  <li>
  <%= link_to "Show", user_path(user.id)  %>
  </li>
</div>

<% end %>

<%= render 'books/list', user: @user, book: @book %>